<h3>Here are the definitions for this word:</h3>

<ul>
  <% @word.explanations().each() do |definition| %>
    <li><a href = "/definitions/<%= definition.id()%>"><%= car.description()%><%= car.partofspeech()%></a><li>
  <% end %>
</ul>

<a href = "/words/<%= definition.id()%>"><%= @word.id%>/definitions/new">Add a New Definition</a>
<a href = "/">Return to Main Page</a>
